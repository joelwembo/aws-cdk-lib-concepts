import { Construct } from 'constructs';
import { Chart, ChartProps } from 'cdk8s';
export declare class MyChart extends Chart {
    constructor(scope: Construct, id: string, props?: ChartProps);
}
